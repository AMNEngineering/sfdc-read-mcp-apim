<policies>
    <inbound>
        <base />

        <!-- Simple pass-through for now - will add security features incrementally -->
        <set-backend-service backend-id="backend-sfdc-read-${environment}" />
    </inbound>

    <backend>
        <base />
    </backend>

    <outbound>
        <base />
    </outbound>

    <on-error>
        <base />
    </on-error>
</policies>
